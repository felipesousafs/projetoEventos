<%= content_for :full_box do %>opa
<% end %>

<div class="box box-primary">
  <div class="box-header with-border">
    <h2 class="box-title">
        <i class="fa fa-fw fa-list"></i> Responsável pela atividade <%= link_to @responsible.event_item.name, @responsible.event_item.event, class: 'label label-default flat ' %>
    </h2>
  </div>
  <div class="box-body no-padding">

    <table class="table table-striped table-bordered">
      <tr>
        <td width="20%"><strong><%= Responsible.human_attribute_name(:name) %>:</strong></td>
        <td width="30%"><%= @responsible.name %></td>
        <td width="20%"><strong><%= Responsible.human_attribute_name(:responsibility_type) %>:</strong></td>
        <td width="30%"><%= @responsible.responsibility_type.name %></td>
      </tr>
      <tr>
        <td width="20%"><strong><%= Responsible.human_attribute_name(:description) %>:</strong></td>
        <td width="80%" colspan="3"><%= @responsible.description %></td>
      </tr>
    </table>
  </div>
</div>