<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>

  <%= f.error_notification %>
  <p class="login-box-msg">Insira seus dados para cadastro.</p>
  <div class="margin-bottom">
    <%= f.input :name, required: true, autofocus: true %>
    <%= f.input :email, required: true %>
    <%= f.input :password, required: true, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
    <%= f.input :password_confirmation, required: true %>
    <%= f.input 'tags_list', collection: ActsAsTaggableOn::Tag.all, value_method: :name, input_html: { class: 'select2-field',  multiple: 'multiple' }, as: :select %>
  </div>

  <div class="row">
    <div class="col-xs-4">
      <%= button_tag class: 'btn btn-default btn-flat' do %>
        <i class="fa fa-lock" aria-hidden="true"></i>&nbsp;Cadastrar
      <% end %>
    </div>
    <div class="col-xs-12">
      <hr class="divider" />
      <%= render "devise/shared/links" %>
    </div>
  </div>
<% end %>